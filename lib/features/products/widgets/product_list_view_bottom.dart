import 'package:flutter/material.dart';

class ProductListViewBottom extends StatelessWidget {
  const ProductListViewBottom({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.amber);
  }
}
